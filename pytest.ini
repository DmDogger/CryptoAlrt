[tool:pytest]
pythonpath = price_tracking_service notification_service/src siws_login_service/src portfolio_tracker/src
asyncio_mode = auto
markers =
    asyncio: marks tests as async (deselect with '-m "not asyncio"')